#!/usr/bin/env node

require('tsconfig-paths').register({
  baseUrl: __dirname + '/../dist/src/app',
  paths: {
    '@/*': ['./*'],
  },
});

const { program } = require('../dist/src/app/index.js');
program.parse(process.argv);
