enum ExecutionStatus {
  Pending = 'pending',
  Running = 'running',
  Completed = 'completed',
  Failed = 'failed',
  Timeout = 'timeout',
  Cancelled = 'cancelled',
}

export default ExecutionStatus;
